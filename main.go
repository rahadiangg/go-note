package main

import "github.com/rahadiangg/go-note/cmd"

func main() {

	// location := "/tmp/"
	// fileName := "go-note.json"
	// path := utils.JoinPathFile(location, fileName)

	// utils.CreateFile(path)

	// // utils.WriteNote("dodo", "Makan siang enak sekali", path)
	// utils.ReadNote(path)

	// utils.DeleteNote(4, path)

	// utils.ReadNote(path)
	cmd.Execute()
}
